<div class="container">
  <h1 class="h1 mt-3 font-weight-bold text-center">Un petit oubli ?</h1>
  <div class="row">
    <div class="col-md-6 offset-md-3">
      <br><br><br>
      <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
        <small><%= render "devise/shared/error_messages", resource: resource %></small>

        <div class="form-group">
          <%= f.label :email %><br />
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
        </div>

        <div class="actions mt-5">
          <%= f.submit "Réinitialiser mon mot de passe", class: "btn btn-lg btn-primary btn-block" %>
        </div>
      <% end %>
      <br /><br /><br />
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>